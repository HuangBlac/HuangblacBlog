# seed

这里保存项目写作素材与可复用的本地 Skill。

- `shi-guan/`：史官 Skill 源码、数据契约和测试，可纳入版本审查。
- `.shi-guan/`：私有事件事实源与扫描游标，默认忽略。
- `*传.md`：由事实源生成的人读传记，默认忽略。
- `packages/`：生成的 `.skill` 安装包，默认忽略。
- `initial-events/`：首批导入材料，默认忽略。

私有材料不会进入站点构建；忽略规则只防止误提交，不代替备份与敏感信息检查。
